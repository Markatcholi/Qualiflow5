import "./globals.css";

export const metadata = {
  title: "QualiFlow",
  description: "CAPA and NCMR SaaS starter"
};

export default function RootLayout({
  children,
}: {
 return ( 
   <html lang="en">
     <body>{children}</body>
   </html>
  ); 
}  
